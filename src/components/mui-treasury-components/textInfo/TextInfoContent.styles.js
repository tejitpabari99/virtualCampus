export default ({ palette }) => ({
  overline: {
    fontSize: '0.75rem',
    textTransform: 'uppercase',
    color: palette.text.secondary,
  },
  heading: {
    fontSize: '1rem',
    fontWeight: 'bold',
  },
  body: {
    color: 'rgba(0, 0, 0, 0.7)',
  },
});
